import ChoiceMenu.Menu;

public class Main {

    public static void main(String[] args) {
        /*It is better to make one static method in Menu() class.
        Because in this way you just simple create class object, but don't use it at all.*/
        Menu menu = new Menu();
    }
}
